const VestingHasDistributorHandler = artifacts.require('./VestingHasDistributorHandler.sol');

contract('VestingHasDistributorHandler', () => {
  it('Deploys', async () => {

  });

  it('Releases and distributes when called directly', async () => {

  });

  it('Releases and distributes when called from the callback', async () => {

  })
})
